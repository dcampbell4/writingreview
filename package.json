{
  "name": "writing-profiles",
  "private": true,
  "type": "module",
  "description": "Longitudinal student-writing anomaly screener for teachers. Static site: no build step, no server.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "python3 -m http.server 8000"
  }
}
